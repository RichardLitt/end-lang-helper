/* eslint-env mocha */
const assert = require('assert')
const addStars = require('../.')

// TODO Remove this tests and add real ones
describe('Add Stars', function () {
  it('should return 1', function () {
    assert.equal(1, addStars())
  })
})
